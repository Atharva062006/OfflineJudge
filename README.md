Clone or Download the file



Use this for batch file:
@echo off
python "/path" %*



Other commands:
python -m pip install rich
type nul > judge.bat (create a batch file using cmd)


Check for: 
Python
gcc
g++
java
